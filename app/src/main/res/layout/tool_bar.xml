<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="@dimen/size88">

    <Space
        android:id="@+id/space"
        android:layout_width="@dimen/size10"
        android:layout_height="@dimen/size24"
        android:layout_marginBottom="@dimen/size10"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <androidx.appcompat.widget.AppCompatImageButton
        android:id="@+id/btnBack"
        android:layout_width="@dimen/size24"
        android:layout_height="@dimen/size24"
        android:layout_gravity="center_vertical"
        android:layout_marginBottom="@dimen/size10"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:src="@drawable/ic_back"
        android:visibility="visible"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toEndOf="@+id/space" />

    <!-- Title căn chính giữa toàn màn hình (parent) -->
    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/tvTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="@dimen/size60"
        android:ellipsize="end"
        android:gravity="center"
        android:includeFontPadding="false"
        android:maxLines="1"
        android:text="@string/app_name"
        android:textColor="@color/white"
        android:textSize="@dimen/size18"
        app:fontFamily="@font/plus_jakarta_sans_semi_bold"
        app:layout_constraintBottom_toBottomOf="@+id/space"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@+id/space" />

    <!-- Nút Action góc phải (Save / Done) -->
    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/btnAction"
        android:layout_width="wrap_content"
        android:layout_height="@dimen/size40"
        android:layout_marginEnd="@dimen/size10"
        android:background="@drawable/background_transparent_selector"
        android:gravity="center"
        android:paddingHorizontal="@dimen/size15"
        android:text="@string/save"
        android:textAllCaps="false"
        android:textColor="@color/action_toolbar"
        android:textSize="@dimen/size16"
        android:visibility="gone"
        app:fontFamily="@font/plus_jakarta_sans_semi_bold"
        app:layout_constraintBottom_toBottomOf="@+id/space"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@+id/space" />

</androidx.constraintlayout.widget.ConstraintLayout>
