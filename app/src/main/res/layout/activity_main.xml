<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background_main">

    <!-- Top Bar: Title and Settings Button -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/layoutHeader"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingHorizontal="20dp"
        android:paddingTop="36dp"
        android:paddingBottom="12dp"
        app:layout_constraintTop_toTopOf="parent">

        <androidx.appcompat.widget.AppCompatTextView
            android:id="@+id/tvHeaderTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="@font/plus_jakarta_sans_semi_bold"
            android:includeFontPadding="false"
            android:text="@string/flashlightTitle"
            android:textColor="@color/white"
            android:textSize="20sp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <androidx.appcompat.widget.AppCompatImageButton
            android:id="@+id/btnSettings"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="@drawable/bg_setting_circle"
            android:padding="10dp"
            android:scaleType="fitCenter"
            android:src="@drawable/ic_setting"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:tint="@color/gray3" />

    </androidx.constraintlayout.widget.ConstraintLayout>

    <!-- Center Power Button with Glow Layer -->
    <FrameLayout
        android:id="@+id/containerPower"
        android:layout_width="260dp"
        android:layout_height="260dp"
        app:layout_constraintBottom_toTopOf="@+id/layoutModeSelector"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/layoutHeader"
        app:layout_constraintVertical_bias="0.38">

        <!-- Outer Halo Glow Layer (Visible only when ON) -->
        <View
            android:id="@+id/viewPowerGlow"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_gravity="center"
            android:background="@drawable/bg_power_glow_flashlight"
            android:visibility="gone"
            tools:visibility="visible" />

        <!-- Circular Main Power Button -->
        <LinearLayout
            android:id="@+id/btnPower"
            android:layout_width="160dp"
            android:layout_height="160dp"
            android:layout_gravity="center"
            android:background="@drawable/bg_power_button_off"
            android:clickable="true"
            android:focusable="true"
            android:gravity="center"
            android:orientation="vertical">

            <androidx.appcompat.widget.AppCompatImageView
                android:id="@+id/ivPowerIcon"
                android:layout_width="80dp"
                android:layout_height="80dp"
                android:src="@drawable/ic_flashlight"
                app:tint="@color/gray" />

            <androidx.appcompat.widget.AppCompatTextView
                android:id="@+id/tvPowerState"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:fontFamily="@font/plus_jakarta_sans_semi_bold"
                android:includeFontPadding="false"
                android:letterSpacing="0.05"
                android:text="@string/state_off"
                android:textAllCaps="true"
                android:textColor="@color/gray"
                android:textSize="20sp" />
        </LinearLayout>
    </FrameLayout>

    <!-- 3-Mode Selector Segment -->
    <LinearLayout
        android:id="@+id/layoutModeSelector"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="10dp"
        android:layout_marginBottom="24dp"
        android:gravity="center"
        android:orientation="horizontal"
        android:weightSum="3"
        app:layout_constraintBottom_toTopOf="@+id/adContainer"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent">

        <!-- Mode 1: Flashlight -->
        <LinearLayout
            android:id="@+id/btnModeFlashlight"
            style="@style/StyleModeTabContainer"
            android:layout_marginEnd="6dp">

            <androidx.appcompat.widget.AppCompatImageView
                android:id="@+id/ivModeFlashlight"
                style="@style/StyleModeTabIcon"
                android:src="@drawable/ic_flashlight" />

            <androidx.appcompat.widget.AppCompatTextView
                android:id="@+id/tvModeFlashlight"
                style="@style/StyleModeTabText"
                android:text="@string/flashlight" />
        </LinearLayout>

        <!-- Mode 2: SOS Light -->
        <LinearLayout
            android:id="@+id/btnModeSos"
            style="@style/StyleModeTabContainer"
            android:layout_marginHorizontal="3dp">

            <androidx.appcompat.widget.AppCompatImageView
                android:id="@+id/ivModeSos"
                style="@style/StyleModeTabIcon"
                android:src="@drawable/ic_soslight" />

            <androidx.appcompat.widget.AppCompatTextView
                android:id="@+id/tvModeSos"
                style="@style/StyleModeTabText"
                android:text="@string/sos_light" />
        </LinearLayout>

        <!-- Mode 3: DJ Mode -->
        <LinearLayout
            android:id="@+id/btnModeDj"
            style="@style/StyleModeTabContainer"
            android:layout_marginStart="6dp">

            <androidx.appcompat.widget.AppCompatImageView
                android:id="@+id/ivModeDj"
                style="@style/StyleModeTabIcon"
                android:src="@drawable/ic_djmode" />

            <androidx.appcompat.widget.AppCompatTextView
                android:id="@+id/tvModeDj"
                style="@style/StyleModeTabText"
                android:text="@string/dj_mode" />
        </LinearLayout>

    </LinearLayout>

    <!-- Ad / Buffer Container -->
    <FrameLayout
        android:id="@+id/adContainer"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintBottom_toTopOf="@+id/bottomNavView"
        app:layout_constraintTop_toBottomOf="@+id/layoutModeSelector" />

    <!-- Bottom Navigation Bar -->
    <include
        android:id="@+id/bottomNavView"
        layout="@layout/layout_main_bottom_navigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
